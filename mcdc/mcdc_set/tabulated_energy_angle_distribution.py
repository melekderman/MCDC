# The following is automatically generated by code_factory.py

from numba import njit


@njit
def energy(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["energy_offset"]
    data[offset + index] = value


@njit
def energy_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["energy_offset"]
    size = tabulated_energy_angle_distribution["energy_length"]
    end = start + size
    data[start:end] = value


@njit
def energy_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["energy_offset"]
    size = tabulated_energy_angle_distribution["energy_length"]
    end = start + size
    data[end - 1] = value


@njit
def energy_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["energy_offset"]
    end = start + length
    data[start:end] = value


@njit
def offset(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["offset_offset"]
    data[offset + index] = value


@njit
def offset_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["offset_offset"]
    size = tabulated_energy_angle_distribution["offset_length"]
    end = start + size
    data[start:end] = value


@njit
def offset_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["offset_offset"]
    size = tabulated_energy_angle_distribution["offset_length"]
    end = start + size
    data[end - 1] = value


@njit
def offset_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["offset_offset"]
    end = start + length
    data[start:end] = value


@njit
def energy_out(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["energy_out_offset"]
    data[offset + index] = value


@njit
def energy_out_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["energy_out_offset"]
    size = tabulated_energy_angle_distribution["energy_out_length"]
    end = start + size
    data[start:end] = value


@njit
def energy_out_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["energy_out_offset"]
    size = tabulated_energy_angle_distribution["energy_out_length"]
    end = start + size
    data[end - 1] = value


@njit
def energy_out_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["energy_out_offset"]
    end = start + length
    data[start:end] = value


@njit
def pdf(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["pdf_offset"]
    data[offset + index] = value


@njit
def pdf_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["pdf_offset"]
    size = tabulated_energy_angle_distribution["pdf_length"]
    end = start + size
    data[start:end] = value


@njit
def pdf_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["pdf_offset"]
    size = tabulated_energy_angle_distribution["pdf_length"]
    end = start + size
    data[end - 1] = value


@njit
def pdf_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["pdf_offset"]
    end = start + length
    data[start:end] = value


@njit
def cdf(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["cdf_offset"]
    data[offset + index] = value


@njit
def cdf_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cdf_offset"]
    size = tabulated_energy_angle_distribution["cdf_length"]
    end = start + size
    data[start:end] = value


@njit
def cdf_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cdf_offset"]
    size = tabulated_energy_angle_distribution["cdf_length"]
    end = start + size
    data[end - 1] = value


@njit
def cdf_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["cdf_offset"]
    end = start + length
    data[start:end] = value


@njit
def cosine_offset_(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["cosine_offset__offset"]
    data[offset + index] = value


@njit
def cosine_offset__all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_offset__offset"]
    size = tabulated_energy_angle_distribution["cosine_offset__length"]
    end = start + size
    data[start:end] = value


@njit
def cosine_offset__last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_offset__offset"]
    size = tabulated_energy_angle_distribution["cosine_offset__length"]
    end = start + size
    data[end - 1] = value


@njit
def cosine_offset__chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["cosine_offset__offset"]
    end = start + length
    data[start:end] = value


@njit
def cosine(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["cosine_offset"]
    data[offset + index] = value


@njit
def cosine_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_offset"]
    size = tabulated_energy_angle_distribution["cosine_length"]
    end = start + size
    data[start:end] = value


@njit
def cosine_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_offset"]
    size = tabulated_energy_angle_distribution["cosine_length"]
    end = start + size
    data[end - 1] = value


@njit
def cosine_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["cosine_offset"]
    end = start + length
    data[start:end] = value


@njit
def cosine_pdf(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["cosine_pdf_offset"]
    data[offset + index] = value


@njit
def cosine_pdf_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_pdf_offset"]
    size = tabulated_energy_angle_distribution["cosine_pdf_length"]
    end = start + size
    data[start:end] = value


@njit
def cosine_pdf_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_pdf_offset"]
    size = tabulated_energy_angle_distribution["cosine_pdf_length"]
    end = start + size
    data[end - 1] = value


@njit
def cosine_pdf_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["cosine_pdf_offset"]
    end = start + length
    data[start:end] = value


@njit
def cosine_cdf(index, tabulated_energy_angle_distribution, data, value):
    offset = tabulated_energy_angle_distribution["cosine_cdf_offset"]
    data[offset + index] = value


@njit
def cosine_cdf_all(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_cdf_offset"]
    size = tabulated_energy_angle_distribution["cosine_cdf_length"]
    end = start + size
    data[start:end] = value


@njit
def cosine_cdf_last(tabulated_energy_angle_distribution, data, value):
    start = tabulated_energy_angle_distribution["cosine_cdf_offset"]
    size = tabulated_energy_angle_distribution["cosine_cdf_length"]
    end = start + size
    data[end - 1] = value


@njit
def cosine_cdf_chunk(start, length, tabulated_energy_angle_distribution, data, value):
    start += tabulated_energy_angle_distribution["cosine_cdf_offset"]
    end = start + length
    data[start:end] = value
